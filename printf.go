package main

import (
	"fmt"
)

func main() {
	name3 := 104
	fmt.Printf("%d",name3) //printf uses differenting formatting options, e.g %d is used for a digit
	fmt.Printf("%c",name3) //printf uses differenting formatting options, e.g %c is used to convert number to hex. Rune!
	// %v is a catch all and tries to figure it out on its own

}